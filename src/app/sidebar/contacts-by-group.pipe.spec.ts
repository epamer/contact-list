import { ContactsByGroupPipe } from './contacts-by-group.pipe';

describe('ContactsByGrouprPipe', () => {
  it('create an instance', () => {
    const pipe = new ContactsByGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
